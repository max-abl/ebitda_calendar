{% extends 'content.html.twig' %}

{% block title %}{{ parent() }} - Edit event{% endblock %}

{% block content %}
    <div class="offset-lg-3 col-lg-6 text-neutral">
        <h4 class="font-weight-bold">EDIT EVENT</h4>
        {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}
        <a class="badge badge-default" href="{{ path('app_index') }}">back to list</a>
        {% if is_granted("ROLE_SUPER_ADMIN") %}
            {{ include('event/_delete_form.html.twig') }}
        {% endif %}
    </div>
{% endblock %}
